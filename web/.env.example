# Leave empty to derive the API base from the page hostname in local
# development (works from localhost, LAN and VPN addresses alike).
API_BASE_URL=
# Public client configuration for the Privy application used by this install.
PRIVY_APP_ID=your-privy-app-id
PRIVY_CLIENT_ID=your-privy-client-id
