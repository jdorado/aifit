# Third-party notices

The AIFit plugin has no runtime npm dependencies.

The included Dockerfile uses the official `node:22-alpine` base image. The
Node.js, V8, and Alpine notices bundled by that image remain governed by their
respective distributions and are available from the image metadata.
